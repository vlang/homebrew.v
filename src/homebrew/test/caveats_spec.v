module test

import brew_runtime

// Translated from Homebrew/brew `test/caveats_spec.rb`.
// The original source is retained below until every stub has a typed V body.

// Ruby subject `subject(:caveats) { described_class.new(f) }` at line 8.
pub fn ruby_caveats_spec_l8_d1_caveats(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('caveats', ...args)
}

// Ruby let `let(:f) do` at line 10.
pub fn ruby_caveats_spec_l10_d2_f(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('f', ...args)
}

// Ruby specify `specify "#f" do` at line 17.
pub fn ruby_caveats_spec_l17_d3_f(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('#f', ...args)
}

// Ruby it `it "returns true if the Formula has no caveats" do` at line 22.
pub fn ruby_caveats_spec_l22_d4_returns(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('returns', ...args)
}

// Ruby it `it "returns false if the Formula has caveats" do` at line 26.
pub fn ruby_caveats_spec_l26_d5_returns(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('returns', ...args)
}

// Ruby method `caveats` at line 31.
pub fn ruby_caveats_spec_l31_d6_caveats(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('caveats', ...args)
}

// Ruby it `it "gives information about service" do` at line 46.
pub fn ruby_caveats_spec_l46_d7_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "prints warning when no service daemon is found" do` at line 62.
pub fn ruby_caveats_spec_l62_d8_prints(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('prints', ...args)
}

// Ruby it `it "prints service startup information when service.require_root is true" do` at line 76.
pub fn ruby_caveats_spec_l76_d9_prints(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('prints', ...args)
}

// Ruby it `it "prints service login information" do` at line 90.
pub fn ruby_caveats_spec_l90_d10_prints(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('prints', ...args)
}

// Ruby it `it "gives information about require_root restarting services after upgrade" do` at line 103.
pub fn ruby_caveats_spec_l103_d11_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives information about user restarting services after upgrade" do` at line 118.
pub fn ruby_caveats_spec_l118_d12_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives information about require_root starting services after upgrade" do` at line 132.
pub fn ruby_caveats_spec_l132_d13_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives information about user starting services after upgrade" do` at line 147.
pub fn ruby_caveats_spec_l147_d14_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives information about service manual command" do` at line 161.
pub fn ruby_caveats_spec_l161_d15_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "prints info when there are custom service files" do` at line 178.
pub fn ruby_caveats_spec_l178_d16_prints(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('prints', ...args)
}

// Ruby let `let(:f) do` at line 194.
pub fn ruby_caveats_spec_l194_d17_f(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('f', ...args)
}

// Ruby let `let(:caveats) { described_class.new(f).caveats }` at line 201.
pub fn ruby_caveats_spec_l201_d18_caveats(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('caveats', ...args)
}

// Ruby it `it "tells formula is keg_only" do` at line 203.
pub fn ruby_caveats_spec_l203_d19_tells(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('tells', ...args)
}

// Ruby it `it "omits keg-only caveats when the formula is linked" do` at line 207.
pub fn ruby_caveats_spec_l207_d20_omits(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('omits', ...args)
}

// Ruby it `it "gives command to be run when f.bin is a directory" do` at line 213.
pub fn ruby_caveats_spec_l213_d21_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives command to be run when f.sbin is a directory" do` at line 218.
pub fn ruby_caveats_spec_l218_d22_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives command to be run when f.lib is a directory" do` at line 224.
pub fn ruby_caveats_spec_l224_d23_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives command to be run when f.include is a directory" do` at line 229.
pub fn ruby_caveats_spec_l229_d24_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby it `it "gives PKG_CONFIG_PATH when f.lib/'pkgconfig' and f.share/'pkgconfig' are directories" do` at line 234.
pub fn ruby_caveats_spec_l234_d25_gives(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('gives', ...args)
}

// Ruby let `let(:f) do` at line 246.
pub fn ruby_caveats_spec_l246_d26_f(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('f', ...args)
}

// Ruby method `caveats` at line 252.
pub fn ruby_caveats_spec_l252_d27_caveats(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('caveats', ...args)
}

// Ruby let `let(:caveats) { described_class.new(f).caveats }` at line 263.
pub fn ruby_caveats_spec_l263_d28_caveats(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('caveats', ...args)
}

// Ruby it `it "adds the correct amount of new lines to the output" do` at line 265.
pub fn ruby_caveats_spec_l265_d29_adds(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('adds', ...args)
}

// Ruby let `let(:f) do` at line 276.
pub fn ruby_caveats_spec_l276_d30_f(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('f', ...args)
}

// Ruby it `it "warns about shadowed executables on PATH in alphabetical order" do` at line 291.
pub fn ruby_caveats_spec_l291_d31_warns(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('warns', ...args)
}

// Ruby it `it "does not warn when PATH resolves to the formula's own executable" do` at line 308.
pub fn ruby_caveats_spec_l308_d32_does(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('does', ...args)
}

// Ruby it `it "does not warn for keg-only formulae" do` at line 315.
pub fn ruby_caveats_spec_l315_d33_does(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('does', ...args)
}

// Ruby it `it "does not warn when the queried formula itself is not installed" do` at line 330.
pub fn ruby_caveats_spec_l330_d34_does(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('does', ...args)
}

// Ruby it `it "warns for a keg-only formula when a sibling keg is linked over it" do` at line 354.
pub fn ruby_caveats_spec_l354_d35_warns(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('warns', ...args)
}

// Ruby it `it "warns when a keg-only formula has been linked" do` at line 380.
pub fn ruby_caveats_spec_l380_d36_warns(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('warns', ...args)
}

// Ruby it `it "does not warn when HOMEBREW_NO_PATH_SHADOW_CHECK is set" do` at line 397.
pub fn ruby_caveats_spec_l397_d37_does(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('does', ...args)
}

// Ruby it `it "shows the opt-out hint by default" do` at line 405.
pub fn ruby_caveats_spec_l405_d38_shows(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('shows', ...args)
}

// Ruby it `it "hides the opt-out hint when HOMEBREW_NO_ENV_HINTS is set" do` at line 413.
pub fn ruby_caveats_spec_l413_d39_hides(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('hides', ...args)
}

// Ruby it `it "annotates sibling-keg shadowers with the keg name and adds a `brew link` hint" do` at line 422.
pub fn ruby_caveats_spec_l422_d40_annotates(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('annotates', ...args)
}

// Ruby it `it "annotates only the sibling line when shadowers are mixed" do` at line 439.
pub fn ruby_caveats_spec_l439_d41_annotates(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('annotates', ...args)
}

// Ruby let `let(:f) do` at line 465.
pub fn ruby_caveats_spec_l465_d42_f(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('f', ...args)
}

// Ruby let `let(:caveats) { described_class.new(f) }` at line 471.
pub fn ruby_caveats_spec_l471_d43_caveats(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('caveats', ...args)
}

// Ruby let `let(:path) { f.prefix.resolved_path }` at line 472.
pub fn ruby_caveats_spec_l472_d44_path(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('path', ...args)
}

// Ruby let `let(:bash_completion_dir) { path/"etc/bash_completion.d" }` at line 474.
pub fn ruby_caveats_spec_l474_d45_bash_completion_dir(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('bash_completion_dir', ...args)
}

// Ruby let `let(:fish_vendor_completions) { path/"share/fish/vendor_completions.d" }` at line 475.
pub fn ruby_caveats_spec_l475_d46_fish_vendor_completions(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('fish_vendor_completions', ...args)
}

// Ruby let `let(:zsh_site_functions) { path/"share/zsh/site-functions" }` at line 476.
pub fn ruby_caveats_spec_l476_d47_zsh_site_functions(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('zsh_site_functions', ...args)
}

// Ruby let `let(:pwsh_completion_dir) { path/"share/pwsh/completions" }` at line 477.
pub fn ruby_caveats_spec_l477_d48_pwsh_completion_dir(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('pwsh_completion_dir', ...args)
}

// Ruby it `it "includes where Bash completions have been installed to" do` at line 487.
pub fn ruby_caveats_spec_l487_d49_includes(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('includes', ...args)
}

// Ruby it `it "includes where fish completions have been installed to" do` at line 493.
pub fn ruby_caveats_spec_l493_d50_includes(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('includes', ...args)
}

// Ruby it `it "includes where zsh completions have been installed to" do` at line 499.
pub fn ruby_caveats_spec_l499_d51_includes(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('includes', ...args)
}

// Ruby it `it "includes where pwsh completions have been installed to" do` at line 505.
pub fn ruby_caveats_spec_l505_d52_includes(args ...brew_runtime.Value) brew_runtime.Value {
	return brew_runtime.unimplemented_fn('includes', ...args)
}

// Original Ruby source (line-for-line):
// 1: # typed: true
// 2: # frozen_string_literal: true
// 3:
// 4: require "formula"
// 5: require "caveats"
// 6:
// 7: RSpec.describe Caveats do
// 8:   subject(:caveats) { described_class.new(f) }
// 9:
// 10:   let(:f) do
// 11:     formula do
// 12:       T.bind(self, T.class_of(Formula))
// 13:       url "foo-1.0"
// 14:     end
// 15:   end
// 16:
// 17:   specify "#f" do
// 18:     expect(caveats.formula).to eq(f)
// 19:   end
// 20:
// 21:   describe "#empty?" do
// 22:     it "returns true if the Formula has no caveats" do
// 23:       expect(caveats).to be_empty
// 24:     end
// 25:
// 26:     it "returns false if the Formula has caveats" do
// 27:       f = formula do
// 28:         T.bind(self, T.class_of(Formula))
// 29:         url "foo-1.0"
// 30:
// 31:         def caveats
// 32:           "something"
// 33:         end
// 34:       end
// 35:
// 36:       expect(described_class.new(f)).not_to be_empty
// 37:     end
// 38:   end
// 39:
// 40:   describe "#caveats" do
// 41:     context "when service block is defined" do
// 42:       before do
// 43:         allow(Utils::Service).to receive_messages(launchctl?: true, systemctl?: true)
// 44:       end
// 45:
// 46:       it "gives information about service" do
// 47:         f = formula do
// 48:           T.bind(self, T.class_of(Formula))
// 49:           url "foo-1.0"
// 50:           service do
// 51:             T.bind(self, Homebrew::Service)
// 52:             run [bin/"php", "test"]
// 53:           end
// 54:         end
// 55:         caveats = described_class.new(f).caveats
// 56:
// 57:         expect(f.service?).to be(true)
// 58:         expect(caveats).to include("#{f.bin}/php test")
// 59:         expect(caveats).to include("background service")
// 60:       end
// 61:
// 62:       it "prints warning when no service daemon is found" do
// 63:         f = formula do
// 64:           T.bind(self, T.class_of(Formula))
// 65:           url "foo-1.0"
// 66:           service do
// 67:             T.bind(self, Homebrew::Service)
// 68:             run [bin/"cmd"]
// 69:           end
// 70:         end
// 71:         expect(Utils::Service).to receive(:launchctl?).and_return(false)
// 72:         expect(Utils::Service).to receive(:systemctl?).and_return(false)
// 73:         expect(described_class.new(f).caveats).to include("service which can only be used on macOS or systemd!")
// 74:       end
// 75:
// 76:       it "prints service startup information when service.require_root is true" do
// 77:         f = formula do
// 78:           T.bind(self, T.class_of(Formula))
// 79:           url "foo-1.0"
// 80:           service do
// 81:             T.bind(self, Homebrew::Service)
// 82:             run [bin/"cmd"]
// 83:             require_root true
// 84:           end
// 85:         end
// 86:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(false)
// 87:         expect(described_class.new(f).caveats).to include("startup")
// 88:       end
// 89:
// 90:       it "prints service login information" do
// 91:         f = formula do
// 92:           T.bind(self, T.class_of(Formula))
// 93:           url "foo-1.0"
// 94:           service do
// 95:             T.bind(self, Homebrew::Service)
// 96:             run [bin/"cmd"]
// 97:           end
// 98:         end
// 99:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(false)
// 100:         expect(described_class.new(f).caveats).to include("restart at login")
// 101:       end
// 102:
// 103:       it "gives information about require_root restarting services after upgrade" do
// 104:         f = formula do
// 105:           T.bind(self, T.class_of(Formula))
// 106:           url "foo-1.0"
// 107:           service do
// 108:             T.bind(self, Homebrew::Service)
// 109:             run [bin/"cmd"]
// 110:             require_root true
// 111:           end
// 112:         end
// 113:         f_obj = described_class.new(f)
// 114:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(true)
// 115:         expect(f_obj.caveats).to include("  sudo brew services restart #{f.full_name}")
// 116:       end
// 117:
// 118:       it "gives information about user restarting services after upgrade" do
// 119:         f = formula do
// 120:           T.bind(self, T.class_of(Formula))
// 121:           url "foo-1.0"
// 122:           service do
// 123:             T.bind(self, Homebrew::Service)
// 124:             run [bin/"cmd"]
// 125:           end
// 126:         end
// 127:         f_obj = described_class.new(f)
// 128:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(true)
// 129:         expect(f_obj.caveats).to include("  brew services restart #{f.full_name}")
// 130:       end
// 131:
// 132:       it "gives information about require_root starting services after upgrade" do
// 133:         f = formula do
// 134:           T.bind(self, T.class_of(Formula))
// 135:           url "foo-1.0"
// 136:           service do
// 137:             T.bind(self, Homebrew::Service)
// 138:             run [bin/"cmd"]
// 139:             require_root true
// 140:           end
// 141:         end
// 142:         f_obj = described_class.new(f)
// 143:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(false)
// 144:         expect(f_obj.caveats).to include("  sudo brew services start #{f.full_name}")
// 145:       end
// 146:
// 147:       it "gives information about user starting services after upgrade" do
// 148:         f = formula do
// 149:           T.bind(self, T.class_of(Formula))
// 150:           url "foo-1.0"
// 151:           service do
// 152:             T.bind(self, Homebrew::Service)
// 153:             run [bin/"cmd"]
// 154:           end
// 155:         end
// 156:         f_obj = described_class.new(f)
// 157:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(false)
// 158:         expect(f_obj.caveats).to include("  brew services start #{f.full_name}")
// 159:       end
// 160:
// 161:       it "gives information about service manual command" do
// 162:         f = formula do
// 163:           T.bind(self, T.class_of(Formula))
// 164:           url "foo-1.0"
// 165:           service do
// 166:             T.bind(self, Homebrew::Service)
// 167:             run [bin/"cmd", "start"]
// 168:             environment_variables VAR: "foo"
// 169:           end
// 170:         end
// 171:         cmd = "#{HOMEBREW_CELLAR}/formula_name/1.0/bin/cmd"
// 172:         caveats = described_class.new(f).caveats
// 173:
// 174:         expect(caveats).to include("if you don't want/need a background service")
// 175:         expect(caveats).to include("VAR=\"foo\" #{cmd} start")
// 176:       end
// 177:
// 178:       it "prints info when there are custom service files" do
// 179:         f = formula do
// 180:           T.bind(self, T.class_of(Formula))
// 181:           url "foo-1.0"
// 182:           service do
// 183:             T.bind(self, Homebrew::Service)
// 184:             name macos: "custom.mxcl.foo", linux: "custom.foo"
// 185:           end
// 186:         end
// 187:         expect(Utils::Service).to receive(:installed?).with(f).once.and_return(true)
// 188:         expect(Utils::Service).to receive(:running?).with(f).once.and_return(false)
// 189:         expect(described_class.new(f).caveats).to include("restart at login")
// 190:       end
// 191:     end
// 192:
// 193:     context "when f.keg_only is not nil" do
// 194:       let(:f) do
// 195:         formula do
// 196:           T.bind(self, T.class_of(Formula))
// 197:           url "foo-1.0"
// 198:           keg_only "some reason"
// 199:         end
// 200:       end
// 201:       let(:caveats) { described_class.new(f).caveats }
// 202:
// 203:       it "tells formula is keg_only" do
// 204:         expect(caveats).to include("keg-only")
// 205:       end
// 206:
// 207:       it "omits keg-only caveats when the formula is linked" do
// 208:         allow(f).to receive(:linked?).and_return(true)
// 209:
// 210:         expect(caveats).to be_empty
// 211:       end
// 212:
// 213:       it "gives command to be run when f.bin is a directory" do
// 214:         Pathname.new(f.bin).mkpath
// 215:         expect(caveats).to include(f.opt_bin.to_s)
// 216:       end
// 217:
// 218:       it "gives command to be run when f.sbin is a directory" do
// 219:         Pathname.new(f.sbin).mkpath
// 220:         expect(caveats).to include(f.opt_sbin.to_s)
// 221:       end
// 222:
// 223:       context "when f.lib or f.include is a directory" do
// 224:         it "gives command to be run when f.lib is a directory" do
// 225:           Pathname.new(f.lib).mkpath
// 226:           expect(caveats).to include("-L#{f.opt_lib}")
// 227:         end
// 228:
// 229:         it "gives command to be run when f.include is a directory" do
// 230:           Pathname.new(f.include).mkpath
// 231:           expect(caveats).to include("-I#{f.opt_include}")
// 232:         end
// 233:
// 234:         it "gives PKG_CONFIG_PATH when f.lib/'pkgconfig' and f.share/'pkgconfig' are directories" do
// 235:           allow_any_instance_of(Object).to receive(:which).with(any_args).and_return(Pathname.new("blah"))
// 236:
// 237:           Pathname.new(f.share/"pkgconfig").mkpath
// 238:           Pathname.new(f.lib/"pkgconfig").mkpath
// 239:
// 240:           expect(caveats).to include("#{f.opt_lib}/pkgconfig")
// 241:           expect(caveats).to include("#{f.opt_share}/pkgconfig")
// 242:         end
// 243:       end
// 244:
// 245:       context "when joining different caveat types together" do
// 246:         let(:f) do
// 247:           formula do
// 248:             T.bind(self, T.class_of(Formula))
// 249:             url "foo-1.0"
// 250:             keg_only "some reason"
// 251:
// 252:             def caveats
// 253:               "something else"
// 254:             end
// 255:
// 256:             service do
// 257:               T.bind(self, Homebrew::Service)
// 258:               run [bin/"cmd"]
// 259:             end
// 260:           end
// 261:         end
// 262:
// 263:         let(:caveats) { described_class.new(f).caveats }
// 264:
// 265:         it "adds the correct amount of new lines to the output" do
// 266:           allow(Utils::Service).to receive_messages(running?: false, systemctl?: true)
// 267:           expect(caveats).to include("something else")
// 268:           expect(caveats).to include("keg-only")
// 269:           expect(caveats).to include("if you don't want/need a background service")
// 270:           expect(caveats.count("\n")).to eq(9)
// 271:         end
// 272:       end
// 273:     end
// 274:
// 275:     describe "PATH shadowing" do
// 276:       let(:f) do
// 277:         formula do
// 278:           T.bind(self, T.class_of(Formula))
// 279:           url "foo-1.0"
// 280:         end
// 281:       end
// 282:
// 283:       before do
// 284:         Pathname.new(f.opt_bin).mkpath
// 285:         FileUtils.touch(f.opt_bin/"foo")
// 286:         FileUtils.chmod(0755, f.opt_bin/"foo")
// 287:         allow(f).to receive(:any_version_installed?).and_return(true)
// 288:         allow_any_instance_of(Object).to receive(:which).and_call_original
// 289:       end
// 290:
// 291:       it "warns about shadowed executables on PATH in alphabetical order" do
// 292:         FileUtils.touch(f.opt_bin/"bar")
// 293:         FileUtils.chmod(0755, f.opt_bin/"bar")
// 294:
// 295:         foo_shadower = Pathname.new("/usr/local/bin/foo")
// 296:         bar_shadower = Pathname.new("/usr/local/bin/bar")
// 297:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(foo_shadower)
// 298:         allow_any_instance_of(Object).to receive(:which).with("bar", ORIGINAL_PATHS).and_return(bar_shadower)
// 299:         allow(foo_shadower).to receive(:realpath).and_return(foo_shadower)
// 300:         allow(bar_shadower).to receive(:realpath).and_return(bar_shadower)
// 301:         allow(f.opt_bin).to receive(:children).and_return([f.opt_bin/"foo", f.opt_bin/"bar"])
// 302:
// 303:         caveats = described_class.new(f).caveats
// 304:         expect(caveats).to include("foo (shadowed by #{foo_shadower})")
// 305:         expect(caveats.index("bar (shadowed by")).to be < caveats.index("foo (shadowed by")
// 306:       end
// 307:
// 308:       it "does not warn when PATH resolves to the formula's own executable" do
// 309:         own = f.opt_bin/"foo"
// 310:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(own)
// 311:
// 312:         expect(described_class.new(f).caveats).not_to include("shadowed")
// 313:       end
// 314:
// 315:       it "does not warn for keg-only formulae" do
// 316:         keg_only_f = formula do
// 317:           T.bind(self, T.class_of(Formula))
// 318:           url "foo-1.0"
// 319:           keg_only "some reason"
// 320:         end
// 321:         Pathname.new(keg_only_f.opt_bin).mkpath
// 322:         FileUtils.touch(keg_only_f.opt_bin/"foo")
// 323:         FileUtils.chmod(0755, keg_only_f.opt_bin/"foo")
// 324:         allow_any_instance_of(Object).to receive(:which)
// 325:           .with("foo", ORIGINAL_PATHS).and_return(Pathname.new("/usr/local/bin/foo"))
// 326:
// 327:         expect(described_class.new(keg_only_f).caveats).not_to include("shadowed")
// 328:       end
// 329:
// 330:       it "does not warn when the queried formula itself is not installed" do
// 331:         uninstalled_f = formula("foo@old") do
// 332:           T.bind(self, T.class_of(Formula))
// 333:           url "foo-1.0"
// 334:           keg_only :versioned_formula
// 335:         end
// 336:         Pathname.new(uninstalled_f.opt_bin).mkpath
// 337:         FileUtils.touch(uninstalled_f.opt_bin/"foo")
// 338:         FileUtils.chmod(0755, uninstalled_f.opt_bin/"foo")
// 339:
// 340:         sibling_keg_bin = HOMEBREW_CELLAR/"foo@2.0/2.0/bin"
// 341:         sibling_keg_bin.mkpath
// 342:         sibling_shadower = sibling_keg_bin/"foo"
// 343:         sibling_shadower.write("#!/bin/sh\n")
// 344:         sibling_shadower.chmod(0755)
// 345:
// 346:         allow(uninstalled_f).to receive_messages(versioned_formulae_names: ["foo@2.0"],
// 347:                                                  unversioned_formula_name: "foo",
// 348:                                                  any_version_installed?:   false)
// 349:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(sibling_shadower)
// 350:
// 351:         expect(described_class.new(uninstalled_f).caveats).not_to include("shadowed")
// 352:       end
// 353:
// 354:       it "warns for a keg-only formula when a sibling keg is linked over it" do
// 355:         keg_only_f = formula("foo@1.0") do
// 356:           T.bind(self, T.class_of(Formula))
// 357:           url "foo-1.0"
// 358:           keg_only :versioned_formula
// 359:         end
// 360:         Pathname.new(keg_only_f.opt_bin).mkpath
// 361:         FileUtils.touch(keg_only_f.opt_bin/"foo")
// 362:         FileUtils.chmod(0755, keg_only_f.opt_bin/"foo")
// 363:
// 364:         sibling_keg_bin = HOMEBREW_CELLAR/"foo@2.0/2.0/bin"
// 365:         sibling_keg_bin.mkpath
// 366:         sibling_shadower = sibling_keg_bin/"foo"
// 367:         sibling_shadower.write("#!/bin/sh\n")
// 368:         sibling_shadower.chmod(0755)
// 369:
// 370:         allow(keg_only_f).to receive_messages(versioned_formulae_names: ["foo@2.0"],
// 371:                                               unversioned_formula_name: "foo",
// 372:                                               any_version_installed?:   true)
// 373:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(sibling_shadower)
// 374:
// 375:         caveats = described_class.new(keg_only_f).caveats
// 376:         expect(caveats).to include("foo (shadowed by #{sibling_shadower} from foo@2.0)")
// 377:         expect(caveats).to include("Run `brew link foo@1.0`")
// 378:       end
// 379:
// 380:       it "warns when a keg-only formula has been linked" do
// 381:         keg_only_f = formula do
// 382:           T.bind(self, T.class_of(Formula))
// 383:           url "foo-1.0"
// 384:           keg_only "some reason"
// 385:         end
// 386:         Pathname.new(keg_only_f.opt_bin).mkpath
// 387:         FileUtils.touch(keg_only_f.opt_bin/"foo")
// 388:         FileUtils.chmod(0755, keg_only_f.opt_bin/"foo")
// 389:         allow(keg_only_f).to receive_messages(linked?: true, any_version_installed?: true)
// 390:         shadower = Pathname.new("/usr/local/bin/foo")
// 391:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(shadower)
// 392:         allow(shadower).to receive(:realpath).and_return(shadower)
// 393:
// 394:         expect(described_class.new(keg_only_f).caveats).to include("foo (shadowed by #{shadower})")
// 395:       end
// 396:
// 397:       it "does not warn when HOMEBREW_NO_PATH_SHADOW_CHECK is set" do
// 398:         shadower = Pathname.new("/usr/local/bin/foo")
// 399:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(shadower)
// 400:         allow(Homebrew::EnvConfig).to receive(:no_path_shadow_check?).and_return(true)
// 401:
// 402:         expect(described_class.new(f).caveats).not_to include("shadowed")
// 403:       end
// 404:
// 405:       it "shows the opt-out hint by default" do
// 406:         shadower = Pathname.new("/usr/local/bin/foo")
// 407:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(shadower)
// 408:         allow(shadower).to receive(:realpath).and_return(shadower)
// 409:
// 410:         expect(described_class.new(f).caveats).to include("HOMEBREW_NO_PATH_SHADOW_CHECK=1")
// 411:       end
// 412:
// 413:       it "hides the opt-out hint when HOMEBREW_NO_ENV_HINTS is set" do
// 414:         shadower = Pathname.new("/usr/local/bin/foo")
// 415:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(shadower)
// 416:         allow(shadower).to receive(:realpath).and_return(shadower)
// 417:         allow(Homebrew::EnvConfig).to receive(:no_env_hints?).and_return(true)
// 418:
// 419:         expect(described_class.new(f).caveats).not_to include("HOMEBREW_NO_PATH_SHADOW_CHECK")
// 420:       end
// 421:
// 422:       it "annotates sibling-keg shadowers with the keg name and adds a `brew link` hint" do
// 423:         sibling_keg_bin = HOMEBREW_CELLAR/"#{f.name}@1.0/1.0/bin"
// 424:         sibling_keg_bin.mkpath
// 425:         sibling_shadower = sibling_keg_bin/"foo"
// 426:         sibling_shadower.write("#!/bin/sh\n")
// 427:         sibling_shadower.chmod(0755)
// 428:
// 429:         allow(f).to receive_messages(versioned_formulae_names: ["#{f.name}@1.0"], unversioned_formula_name: nil)
// 430:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(sibling_shadower)
// 431:
// 432:         caveats = described_class.new(f).caveats
// 433:         expect(caveats).to include("shadowed by other linked Homebrew commands")
// 434:         expect(caveats).to include("foo (shadowed by #{sibling_shadower} from #{f.name}@1.0)")
// 435:         expect(caveats).to include("Run `brew link #{f.name}`")
// 436:         expect(caveats).not_to include("earlier in your PATH")
// 437:       end
// 438:
// 439:       it "annotates only the sibling line when shadowers are mixed" do
// 440:         Pathname.new(f.opt_bin).mkpath
// 441:         FileUtils.touch(f.opt_bin/"bar")
// 442:         FileUtils.chmod(0755, f.opt_bin/"bar")
// 443:
// 444:         sibling_keg_bin = HOMEBREW_CELLAR/"#{f.name}@1.0/1.0/bin"
// 445:         sibling_keg_bin.mkpath
// 446:         sibling_shadower = sibling_keg_bin/"foo"
// 447:         sibling_shadower.write("#!/bin/sh\n")
// 448:         sibling_shadower.chmod(0755)
// 449:
// 450:         bar_shadower = Pathname.new("/usr/local/bin/bar")
// 451:         allow(bar_shadower).to receive(:realpath).and_return(bar_shadower)
// 452:
// 453:         allow(f).to receive_messages(versioned_formulae_names: ["#{f.name}@1.0"], unversioned_formula_name: nil)
// 454:         allow_any_instance_of(Object).to receive(:which).with("foo", ORIGINAL_PATHS).and_return(sibling_shadower)
// 455:         allow_any_instance_of(Object).to receive(:which).with("bar", ORIGINAL_PATHS).and_return(bar_shadower)
// 456:
// 457:         caveats = described_class.new(f).caveats
// 458:         expect(caveats).to include("foo (shadowed by #{sibling_shadower} from #{f.name}@1.0)")
// 459:         expect(caveats).to include("bar (shadowed by #{bar_shadower})")
// 460:         expect(caveats).to include("Run `brew link #{f.name}`")
// 461:       end
// 462:     end
// 463:
// 464:     describe "shell completions" do
// 465:       let(:f) do
// 466:         formula do
// 467:           T.bind(self, T.class_of(Formula))
// 468:           url "foo-1.0"
// 469:         end
// 470:       end
// 471:       let(:caveats) { described_class.new(f) }
// 472:       let(:path) { f.prefix.resolved_path }
// 473:
// 474:       let(:bash_completion_dir) { path/"etc/bash_completion.d" }
// 475:       let(:fish_vendor_completions) { path/"share/fish/vendor_completions.d" }
// 476:       let(:zsh_site_functions) { path/"share/zsh/site-functions" }
// 477:       let(:pwsh_completion_dir) { path/"share/pwsh/completions" }
// 478:
// 479:       before do
// 480:         # don't try to load/fetch gcc/glibc
// 481:         allow(DevelopmentTools).to receive_messages(needs_libc_formula?: false, needs_compiler_formula?: false)
// 482:
// 483:         allow_any_instance_of(Object).to receive(:which).with(any_args).and_return(Pathname.new("shell"))
// 484:         allow(Utils::Shell).to receive_messages(preferred: nil, parent: nil)
// 485:       end
// 486:
// 487:       it "includes where Bash completions have been installed to" do
// 488:         bash_completion_dir.mkpath
// 489:         FileUtils.touch bash_completion_dir/f.name
// 490:         expect(caveats.completions_and_elisp.join).to include(HOMEBREW_PREFIX/"etc/bash_completion.d")
// 491:       end
// 492:
// 493:       it "includes where fish completions have been installed to" do
// 494:         fish_vendor_completions.mkpath
// 495:         FileUtils.touch fish_vendor_completions/f.name
// 496:         expect(caveats.completions_and_elisp.join).to include(HOMEBREW_PREFIX/"share/fish/vendor_completions.d")
// 497:       end
// 498:
// 499:       it "includes where zsh completions have been installed to" do
// 500:         zsh_site_functions.mkpath
// 501:         FileUtils.touch zsh_site_functions/f.name
// 502:         expect(caveats.completions_and_elisp.join).to include(HOMEBREW_PREFIX/"share/zsh/site-functions")
// 503:       end
// 504:
// 505:       it "includes where pwsh completions have been installed to" do
// 506:         pwsh_completion_dir.mkpath
// 507:         FileUtils.touch pwsh_completion_dir/f.name
// 508:         expect(caveats.completions_and_elisp.join).to include(HOMEBREW_PREFIX/"share/pwsh/completions")
// 509:       end
// 510:     end
// 511:   end
// 512: end
